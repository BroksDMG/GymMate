{
  "name": "gymmate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm run dev",
    "clientBiuld": "cd client && npm run build",
    "api": "cd api && nodemon index.js",
    "dev": "concurrently \"npm run client\" \"npm run api\"",
    "build": "concurrently \"npm run clientBiuld\" \"npm run api\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "multer": "^1.4.5-lts.1"
  }
}
