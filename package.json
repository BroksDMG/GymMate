{
  "name": "gymmate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm run dev",
    "clientBiuld": "cd client && npm install",
    "api": "cd api && nodemon index.js",
    "dev": "concurrently \"npm run client\" \"npm run api\"",
    "build":"cd api && npm install"
    
    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}